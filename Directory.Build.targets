<Project>
    <Target
        Name="UpdateLockFiles"
        AfterTargets="Restore"
        Condition="$(NUKEEPER) == 'true'">

        <MSBuild
            Projects="$(MSBuildThisFileDirectory)lambdajection.sln"
            Targets="Restore"
        />
    </Target>
</Project>