<Project>
    <Target Name="UpdatePermissions" AfterTargets="Publish" Condition="!$([MSBuild]::IsOSPlatform('Windows'))">
        <Exec Command="chmod +x $(PublishDir)/$(AssemblyName)" />
    </Target>
</Project>